apiVersion: v1
kind: Service
metadata:
    name: rpm-repo-svc
spec:
    type: ClusterIP
    selector:
        app: rpm-repo
    ports:
     - name: http
        protocol: TCP
        port: 80
        targetPort: 80

    